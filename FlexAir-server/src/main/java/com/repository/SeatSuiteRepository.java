package com.repository;

import com.model.entity.SeatSuite;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SeatSuiteRepository extends JpaRepository<SeatSuite, Integer> {
}
